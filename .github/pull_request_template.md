## Summary

- 

## Test Plan

- [ ] `just check`
- [ ] `just lint`

## Issue-centered V1 Visual Parity

Required for PRs that touch `/issues`, `/issues/:id`, `/tasks/:id`, `/runs/:id`, or execution drawer UI.

- [ ] Ran `just visual-parity --states <changed-states>`.
- [ ] Attached `mockup.png`, `app.png`, and `diff.png` or `notes.md` for every changed state.
- [ ] Copied the relevant rows from `.visual-parity-output/latest/summary.md` into this PR.
- [ ] Explained every mismatch above threshold or dimension-mismatch note.
- [ ] Confirmed screenshots came from `.visual-parity-output/`, not `~/Downloads`.
